#include<iostream>
using namespace std;
int main(){

    float num, paisa;
    int rupee;

    cin>>num;
    rupee = num;
    paisa = (num - rupee);

    cout<<rupee<<" + "<<paisa;

    /*  int   ->[][] 
        float ->[][][][]
    */
    
}